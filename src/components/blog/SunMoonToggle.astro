<button class="sun-moon-toggle" type="button" data-theme-toggle aria-label="Switch theme">
  <span class="sun-moon-sky" aria-hidden="true">
    <span class="sun-moon-inner-shadow"></span>
    <span class="sun-moon-day"></span>
  </span>

  <span class="sun-moon-stars" aria-hidden="true">
    <span class="sun-moon-star sun-moon-star-0"></span>
    <span class="sun-moon-star sun-moon-star-1"></span>
    <span class="sun-moon-star sun-moon-star-2"></span>
    <span class="sun-moon-star sun-moon-star-3"></span>
    <span class="sun-moon-star sun-moon-star-4"></span>
    <span class="sun-moon-star sun-moon-star-5"></span>
    <span class="sun-moon-star sun-moon-star-6"></span>
    <span class="sun-moon-star sun-moon-star-7"></span>
    <span class="sun-moon-star sun-moon-star-8"></span>
    <span class="sun-moon-meteor"></span>
  </span>

  <span class="sun-moon-clouds" aria-hidden="true">
    <span class="sun-moon-cloud sun-moon-cloud-near-0"></span>
    <span class="sun-moon-cloud sun-moon-cloud-near-1"></span>
    <span class="sun-moon-cloud sun-moon-cloud-near-2"></span>
    <span class="sun-moon-cloud sun-moon-cloud-far-0"></span>
    <span class="sun-moon-cloud sun-moon-cloud-far-1"></span>
    <span class="sun-moon-cloud sun-moon-cloud-far-2"></span>
  </span>

  <span class="sun-moon-halo" aria-hidden="true">
    <span class="sun-moon-halo-inner"></span>
    <span class="sun-moon-halo-middle"></span>
    <span class="sun-moon-halo-outer"></span>
  </span>

  <span class="sun-moon-ball" aria-hidden="true">
    <span class="sun-moon-sun"></span>
    <span class="sun-moon-moon">
      <span class="sun-moon-crater sun-moon-crater-0"></span>
      <span class="sun-moon-crater sun-moon-crater-1"></span>
      <span class="sun-moon-crater sun-moon-crater-2"></span>
    </span>
  </span>
</button>

<style>
  .sun-moon-toggle {
    --toggle-height: 28px;
    --toggle-width: calc(var(--toggle-height) * 2.35);
    --ball-size: calc(var(--toggle-height) * 0.82);
    --ball-margin: calc(var(--toggle-height) * 0.1);
    --move-duration: 760ms;

    position: relative;
    display: inline-flex;
    width: var(--toggle-width);
    height: var(--toggle-height);
    flex: 0 0 var(--toggle-width);
    overflow: hidden;
    padding: 0;
    border: 0;
    border-radius: var(--toggle-height);
    background: transparent;
    color: inherit;
    cursor: pointer;
    isolation: isolate;
    outline-offset: 3px;
  }

  .sun-moon-toggle.sun-moon-toggle {
    width: var(--toggle-width);
    height: var(--toggle-height);
    border: 0;
    border-radius: var(--toggle-height);
    background: transparent;
  }

  .sun-moon-toggle:hover.sun-moon-toggle {
    background: transparent;
    color: inherit;
  }

  .sun-moon-toggle:focus-visible {
    outline: 2px solid var(--paper-accent);
  }

  .sun-moon-sky,
  .sun-moon-stars,
  .sun-moon-clouds,
  .sun-moon-halo {
    position: absolute;
    inset: 0;
    overflow: hidden;
    border-radius: inherit;
  }

  .sun-moon-inner-shadow {
    position: absolute;
    inset: 0;
    z-index: 8;
    border-radius: inherit;
    box-shadow:
      inset 0 0 0.45em rgba(0, 0, 0, 0.48),
      inset 0 0 0 1px rgba(255, 255, 255, 0.2);
  }

  .sun-moon-day {
    position: absolute;
    inset: 0;
    background: #5fa2df;
    transition: background-color var(--move-duration) ease;
  }

  :global(:root[data-theme='dark']) .sun-moon-day {
    background: #171e33;
  }

  .sun-moon-stars {
    transform: translateY(-100%);
    transition: transform var(--move-duration) cubic-bezier(0.26, 0.97, 0.2, 1.08);
  }

  :global(:root[data-theme='dark']) .sun-moon-stars {
    transform: translateY(0);
  }

  .sun-moon-star {
    position: absolute;
    width: calc(var(--ball-size) / 7.5);
    height: calc(var(--ball-size) / 7.5);
    transform: scale(1);
    transition: transform 280ms ease;
  }

  .sun-moon-star::before {
    position: absolute;
    inset: 0;
    background: #fff;
    clip-path: polygon(50% 0, 62% 38%, 100% 50%, 62% 62%, 50% 100%, 38% 62%, 0 50%, 38% 38%);
    content: '';
  }

  .sun-moon-star-0 {
    top: 14%;
    left: 18%;
    transform: scale(1.25);
  }

  .sun-moon-star-1 {
    top: 31%;
    left: 9%;
    transform: scale(0.55);
  }

  .sun-moon-star-2 {
    top: 53%;
    left: 22%;
    transform: scale(0.78);
  }

  .sun-moon-star-3 {
    top: 72%;
    left: 13%;
    transform: scale(0.45);
  }

  .sun-moon-star-4 {
    top: 23%;
    left: 51%;
    transform: scale(1.1);
  }

  .sun-moon-star-5 {
    top: 47%;
    left: 41%;
    transform: scale(0.5);
  }

  .sun-moon-star-6 {
    top: 64%;
    left: 52%;
    transform: scale(0.65);
  }

  .sun-moon-star-7 {
    top: 78%;
    left: 45%;
    transform: scale(0.8);
  }

  .sun-moon-star-8 {
    top: 20%;
    left: 39%;
    transform: scale(0.42);
  }

  .sun-moon-star-1,
  .sun-moon-star-3,
  .sun-moon-star-5 {
    animation: sun-moon-twinkle 1100ms ease-in-out infinite;
  }

  .sun-moon-meteor {
    position: absolute;
    top: -20%;
    left: 78%;
    width: 1px;
    height: 52%;
    border-radius: 999px;
    background: linear-gradient(0deg, rgba(255, 255, 255, 0), rgba(255, 255, 255, 0.95));
    opacity: 0;
    transform: rotate(255deg);
  }

  :global(:root[data-theme='dark']) .sun-moon-meteor {
    animation: sun-moon-meteor-fall 2600ms linear infinite;
  }

  .sun-moon-clouds {
    transition: transform calc(var(--move-duration) * 1.05) cubic-bezier(0.49, 1.57, 0.28, 0.81);
  }

  :global(:root[data-theme='dark']) .sun-moon-clouds {
    transform: translateY(100%);
  }

  .sun-moon-clouds {
    animation: sun-moon-cloud-shake 3800ms ease-in-out infinite;
  }

  .sun-moon-cloud {
    position: absolute;
    border-radius: 50%;
    background: #fff;
  }

  .sun-moon-cloud::before,
  .sun-moon-cloud::after {
    position: absolute;
    border-radius: inherit;
    background: inherit;
    content: '';
  }

  .sun-moon-cloud::before {
    width: 80%;
    height: 80%;
    transform: translate(-45%, 18%);
  }

  .sun-moon-cloud::after {
    width: 68%;
    height: 68%;
    transform: translate(52%, 28%);
  }

  .sun-moon-cloud-near-0 {
    right: -11%;
    bottom: 8%;
    width: calc(var(--ball-size) * 0.76);
    height: calc(var(--ball-size) * 0.76);
  }

  .sun-moon-cloud-near-1 {
    right: 15%;
    bottom: -23%;
    width: calc(var(--ball-size) * 0.95);
    height: calc(var(--ball-size) * 0.95);
  }

  .sun-moon-cloud-near-2 {
    right: 47%;
    bottom: -31%;
    width: calc(var(--ball-size) * 0.88);
    height: calc(var(--ball-size) * 0.88);
  }

  .sun-moon-cloud-far-0,
  .sun-moon-cloud-far-1,
  .sun-moon-cloud-far-2 {
    background: #a8c5e3;
  }

  .sun-moon-cloud-far-0 {
    right: 3%;
    bottom: 35%;
    width: calc(var(--ball-size) * 0.58);
    height: calc(var(--ball-size) * 0.58);
  }

  .sun-moon-cloud-far-1 {
    right: 31%;
    bottom: 14%;
    width: calc(var(--ball-size) * 0.72);
    height: calc(var(--ball-size) * 0.72);
  }

  .sun-moon-cloud-far-2 {
    right: 61%;
    bottom: -3%;
    width: calc(var(--ball-size) * 0.66);
    height: calc(var(--ball-size) * 0.66);
  }

  .sun-moon-halo-inner,
  .sun-moon-halo-middle,
  .sun-moon-halo-outer {
    position: absolute;
    top: 50%;
    left: var(--ball-margin);
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.08);
    transform: translateY(-50%);
    transition: transform var(--move-duration) cubic-bezier(0.26, 0.97, 0.2, 1.08);
  }

  .sun-moon-halo-inner {
    width: calc(var(--toggle-height) * 1.42);
    height: calc(var(--toggle-height) * 1.42);
  }

  .sun-moon-halo-middle {
    width: calc(var(--toggle-height) * 1.74);
    height: calc(var(--toggle-height) * 1.74);
  }

  .sun-moon-halo-outer {
    width: calc(var(--toggle-height) * 2.06);
    height: calc(var(--toggle-height) * 2.06);
  }

  :global(:root[data-theme='dark']) .sun-moon-halo-inner,
  :global(:root[data-theme='dark']) .sun-moon-halo-middle,
  :global(:root[data-theme='dark']) .sun-moon-halo-outer {
    transform: translate(calc(var(--toggle-width) - 100% - (var(--ball-margin) * 2)), -50%);
  }

  .sun-moon-ball {
    position: absolute;
    top: 50%;
    left: var(--ball-margin);
    z-index: 7;
    width: var(--ball-size);
    height: var(--ball-size);
    overflow: hidden;
    border-radius: 50%;
    box-shadow: 0.22em 0.22em 0.36em rgba(0, 0, 0, 0.48);
    transform: translateY(-50%);
    transition: transform var(--move-duration) cubic-bezier(0.26, 0.97, 0.2, 1.08);
  }

  :global(:root[data-theme='dark']) .sun-moon-ball {
    transform: translate(calc(var(--toggle-width) - var(--ball-size) - (var(--ball-margin) * 2)), -50%);
  }

  .sun-moon-sun,
  .sun-moon-moon {
    position: absolute;
    inset: 0;
    border-radius: 50%;
  }

  .sun-moon-sun {
    background: #f3c62b;
    box-shadow:
      inset 0.24em 0.24em 0.28em rgba(255, 255, 255, 0.78),
      inset -0.24em -0.24em 0.8em rgba(0, 0, 0, 0.34);
  }

  .sun-moon-moon {
    background: #c3c9d3;
    box-shadow:
      inset 0.24em 0.24em 0.28em rgba(255, 255, 255, 0.78),
      inset -0.24em -0.24em 0.8em rgba(0, 0, 0, 0.34);
    transform: translateX(100%);
    transition: transform calc(var(--move-duration) / 1.8) ease;
  }

  :global(:root[data-theme='dark']) .sun-moon-moon {
    transform: translateX(0);
  }

  :global(:root[data-theme='dark']) .sun-moon-moon {
    animation: sun-moon-rotate 12s linear infinite;
  }

  .sun-moon-crater {
    position: absolute;
    border-radius: 50%;
    background: #9197a5;
  }

  .sun-moon-crater::before {
    position: absolute;
    inset: 10%;
    border-radius: inherit;
    background: #9ca0b3;
    content: '';
  }

  .sun-moon-crater-0 {
    top: 15%;
    left: 39%;
    width: 18%;
    height: 18%;
  }

  .sun-moon-crater-1 {
    top: 47%;
    left: 14%;
    width: 31%;
    height: 31%;
  }

  .sun-moon-crater-2 {
    top: 62%;
    left: 61%;
    width: 22%;
    height: 22%;
  }

  @keyframes sun-moon-rotate {
    from {
      rotate: 0deg;
    }

    to {
      rotate: 360deg;
    }
  }

  @keyframes sun-moon-cloud-shake {
    0%,
    50%,
    100% {
      translate: 0 0;
    }

    25% {
      translate: 0 3%;
    }

    75% {
      translate: 0 -3%;
    }
  }

  @keyframes sun-moon-twinkle {
    0%,
    100% {
      opacity: 1;
      scale: 1;
    }

    50% {
      opacity: 0.28;
      scale: 0.45;
    }
  }

  @keyframes sun-moon-meteor-fall {
    0%,
    24% {
      opacity: 0;
      translate: 0 0;
    }

    28% {
      opacity: 1;
    }

    52% {
      opacity: 1;
      translate: calc(var(--toggle-width) * -0.95) calc(var(--toggle-height) * 0.58);
    }

    70%,
    100% {
      opacity: 0;
      translate: calc(var(--toggle-width) * -0.95) calc(var(--toggle-height) * 0.58);
    }
  }

  @media (max-width: 720px) {
    .sun-moon-toggle {
      --toggle-height: 25px;
      --toggle-width: calc(var(--toggle-height) * 2.3);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .sun-moon-toggle *,
    .sun-moon-toggle *::before,
    .sun-moon-toggle *::after {
      animation: none !important;
      transition-duration: 1ms !important;
    }
  }
</style>
